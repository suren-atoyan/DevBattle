const existingRoutes = {
  default: 'MONITORING',
  // TODO ::: Add all routes here
}

const url = {
  base_url: '/api/v1.0.0/',
  login: 'login',
  logout: 'logout',
  check_tocken: 'check_token',
}

export { existingRoutes, url };
