const existingRoutes = {
  default: 'MONITORING',
  // TODO ::: Add all routes here
}

const url = {
  base_url: '/api/v1.0.0/',
  login: 'login',
  logout: 'logout',
}

export { existingRoutes, url };
